opencv-python>=4.9.0
numpy>=1.26.0
pywin32>=306
psutil>=5.9.0
PyYAML>=6.0.1
ttkbootstrap>=1.10.1

# สำหรับ build_exe.bat (สร้างไฟล์ .exe) - เปิดคอมเมนต์นี้ที่เครื่องมือผู้ใช้จริงไม่จำเป็นต้องมี
# pyinstaller>=6.12.0

# Optional: เปิดใช้ถ้าต้องการอ่านตัวเลข (เช่น จำนวนเพชร) ด้วย OCR
# ต้องติดตั้งโปรแกรม Tesseract-OCR ในเครื่องด้วย
# pytesseract>=0.3.10
